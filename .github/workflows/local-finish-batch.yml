name: Local Finish Batch

on:
  workflow_dispatch:

jobs:
  local-finish-batch:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          ref: main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run finish batch
        run: |
          python agent/scripts/run_local_finish_batch.py

      - name: Show finish summary
        run: |
          echo "=== finish-batch-summary.json ==="
          cat agent/state/finish-batch-summary.json || true

      - name: Commit finish batch changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add *.html agent/state/*.json || true
          git diff --cached --quiet || git commit -m "Final local cleanup batch"
          git fetch origin main
          git pull --rebase origin main
          git push origin main

      - name: Upload finish reports
        uses: actions/upload-artifact@v4
        with:
          name: local-finish-batch-reports
          path: |
            agent/state/finish-batch-summary.json
            agent/state/page-similarity-report.json
            agent/state/publish-guard-report.json
            agent/state/differentiation-log.json
            agent/state/structural-repair-log.json
